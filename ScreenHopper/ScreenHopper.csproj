<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <StartupObject>ScreenHopper.Program</StartupObject>
    <UseWindowsForms>true</UseWindowsForms>

    <Version>0.2000.001.000000</Version>
    <InformationalVersion>0.2000.001.000000</InformationalVersion>
    <AssemblyVersion>0.2000.001.000000</AssemblyVersion>
    <FileVersion>0.2000.001.000000</FileVersion>
    <ApplicationIcon>ScreenHopper.ico</ApplicationIcon>
    <PackageIcon>ScreenHopper.png</PackageIcon>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)' == 'Release'">
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="ScreenHopper.ico" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\..\..\Downloads\ScreenHopper.png">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Velopack" Version="0.0.1298" />
  </ItemGroup>

  <Target Name="ApplyTimestampVersion" BeforeTargets="GenerateAssemblyInfo" Condition="'$(DesignTimeBuild)' != 'true' and '$(UseTimestampVersion)' != 'false'">
    <PropertyGroup>
      <VersionYear>$([System.DateTime]::Now.ToString('yyyy'))</VersionYear>
      <VersionDayOfYear>$([System.DateTime]::Now.DayOfYear.ToString('000'))</VersionDayOfYear>
      <VersionTime>$([System.DateTime]::Now.ToString('HHmmss'))</VersionTime>

      <Version>0.$(VersionYear).$(VersionDayOfYear).$(VersionTime)</Version>
      <InformationalVersion>$(Version)</InformationalVersion>
      <AssemblyVersion>$(Version)</AssemblyVersion>
      <FileVersion>$(Version)</FileVersion>
    </PropertyGroup>
  </Target>

</Project>
